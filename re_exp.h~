#ifndef __RE_EXP_H
#define __RE_EXP_H

#define OPERAND  1
#define OPERATOR 2

typedef struct re_exp re_exp;
struct re_exp{
  int type;
  int id;
  struct list_head list;
};

#define initreexp(name)				\
  re_exp name;					\
  list_init(name.list)				\
  
re_exp* createreexp();

re_exp* createreexp()
{

}

#endif
